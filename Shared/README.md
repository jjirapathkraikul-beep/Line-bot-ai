# Shared Assets

This directory is reserved for shared AIOS platform assets that are used across multiple applications.

Examples:
- common prompt templates
- reusable schema definitions
- shared tooling scripts
- cross-application policies and connectors
